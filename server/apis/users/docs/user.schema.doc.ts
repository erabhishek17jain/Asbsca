/**
 * @openapi
 * components:
 *   schemas:
 *     User:
 *       type: object
 *       required:
 *         - username
 *         - password
 *         - email
 *         - fullName
 *         - role
 *         - mobile
 *         - address
 *       properties:
 *         username:
 *           type: string
 *         password:
 *           type: string
 *         email:
 *           type: string
 *         fullName:
 *           type: string
 *         role:
 *           type: string
 *         mobile:
 *           type: number
 *         address:
 *           type: string
 *         profile:
 *           type: string
 */
